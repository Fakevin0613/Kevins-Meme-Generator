import { makeStyles} from "@mui/styles";

const navStyle = makeStyles({
    nav: {
        display: 'inline-flex',
        alignItems: 'center',
        color: "primary",
    },
    Ava: {
        marginRight: '30px',
        marginLeft: '30px',
    },
    Typo: {
        userSelect: "none",
    }
});

export default navStyle